The build fails, even though the external dependency is specified
```
       > Setup: dieVerbatim: user error (CallStack (from HasCallStack):
       >   withMetadata, called at libraries/Cabal/Cabal/src/Distribution/Simple/Utils.hs:370:14 in Cabal-3.8.1.0:Distribution.Simple.Utils
       > Error: Setup: The program 'hello' is required but it could not be found.
       > )
       For full logs, run 'nix log /nix/store/afqcs7aimg4w9zsmbs2764h7fxdarwx4-sample-project-source-0.1.0.0.drv'.
```
